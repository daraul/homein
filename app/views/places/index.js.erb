$('#listings.container').append('<%= j render @places %>');
<% if @places.next_page %>
    $('#infiniteScrolling').html('<%= j will_paginate @places %>');
<% else %>
    $(window).off('scroll');
    $('.pagination').remove();
<% end %>