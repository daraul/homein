var content = '<%= j div_for(@places, :class => "listing container") { |place| render place } %>'

$('#listings.container').append(content);

<% if @places.next_page %>
    $('#infiniteScrolling').html('<%= j will_paginate @places %>');
<% else %>
    $(window).off('scroll');
    
    $('.pagination').remove();
<% end %>